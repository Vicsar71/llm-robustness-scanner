# Third-party libraries the project uses.
# Install everything at once with:  pip install -r requirements.txt

httpx>=0.27      # HTTP calls to the local Ollama API
pydantic>=2.5    # typed data models (Attack, Result, Report…)
PyYAML>=6.0      # load the target and attack suites from YAML
typer>=0.12      # command-line interface
rich>=13.0       # nice, colored console output
anthropic>=0.109 # Claude API client for the Claude-based judge (--judge claude)
pytest>=8.0      # tests
