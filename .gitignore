# Python virtual environment (not pushed to GitHub: each dev creates it locally)
.venv/

# Cache Python generates when running
__pycache__/
*.pyc

# Reports/results generated by the scanner (they get regenerated)
reports/
*.log

# Local config and secrets (in case we use API keys later)
.env

# OS files
Thumbs.db
.DS_Store
